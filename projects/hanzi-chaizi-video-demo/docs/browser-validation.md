# 浏览器验收记录

## 运行环境

- 日期：`2026-08-30`（Asia/Shanghai）
- Canonical start command：`python -m http.server 52731 --bind 127.0.0.1 --directory web`
- Canonical URL：<http://127.0.0.1:52731/>
- 服务目录：`projects/hanzi-chaizi-video-demo/web`
- 浏览器：agent-browser 0.27.0 管理的 Chromium
- 支持主题：单一编辑主题；上游章节为深色黑板，“我们的演示”使用浅蓝方格纸作实验对照，没有用户主题切换

R10 保留了七条主视频、照片版横屏／迎宾关键帧、AI 照片故事段，以及桌面／手机完整交付与素材探索实测截图，共 24 份 PNG 证据保存在 `docs/evidence/`；五条场景证据短片及海报保存在 `web/assets/scenario-clips/`。

## 可见问题与修复记录

| Stage | 观察证据 | 根因 | 最小修复 | 邻近检查 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 2/3 | 首屏正常，但全页截图视口外章节为空白 | `.reveal` 默认透明且依赖滚动触发 | 移除内容隐藏和无必要 observer | 全页内容、无脚本/快速滚动语义 | pass |
| 2 | 1440px 标题在“汉/字”之间出现孤行 | 标题最大字号 78px 超过左栏可用宽度 | 最大字号降至 64px | 视频焦点、按钮和证据条 | pass |
| 7 | 768px 双栏压缩标题和判断卡 | 单栏断点设为 720px，平板仍套用桌面布局 | 单栏断点提升到 820px | 视频宽度、后续卡片列数 | pass |
| 7 | 390px 标题再次在“汉/字”之间断开 | 手机字号仍过大且语义行可被浏览器拆分 | 两句分别 `nowrap`，手机字号使用 `32–40px` | 320px 理论最小宽度、横向溢出 | pass |
| R2 / 3 | 新增第二条视频后，双音轨存在同时播放风险 | 两个原生 `<video>` 默认彼此不知道状态 | 统一注册播放事件；任意视频开始时暂停另一条 | 原生控件、两个自定义按钮、键盘焦点 | pass |
| R2 / 7 | 浅色补充演示在窄屏需要保持视频与说明的阅读顺序 | 桌面左视频右说明不能直接压缩 | 820px 以下改为标题、视频、结论和说明单列 | 768/390 截图、卡片列数、下载入口 | pass |
| R3 / 3 | 八类扩展能力容易被读成全部已完成 | 单一“可扩展”标签无法表达成熟度差异 | 分成“已演示／概念验证／待产品化”，并在视频字幕中重复边界 | 页面卡片、视频关键帧、README | pass |
| R3 / 7 | 效果实验室桌面右栏视频在平板会挤压八张说明卡 | 380px 视频与两列卡片无法共同保留可读宽度 | 820px 以下将视频提前为第一行、说明为第二行 | 768/390 顺序、视频宽度、单列卡片 | pass |
| R4 / 3 | 页面有完整章节，但缺少能快速回答用户五个问题的总览 | 结论分散在长页面中，且没有明确单一最佳场景 | 增加能力、原理、最佳场景、扩展和意义五卡总览；把“忙”定位为文化短视频样片 | 五卡链接、场景理由、README | pass |
| R4 / 3 | “儿童教育”容易被理解为唯一产品方向 | 使用场景和产品表没有给出阶段优先级 | 新增近期内容工作台、中期机构内容工具、长期互动学习/API 路线与“现在不做”边界 | 桌面/手机产品意义章节 | pass |
| R4 / 7 | 首次刷新后总览仍显示旧的单列样式 | 浏览器命中未带版本的旧 CSS 缓存 | 为 `styles.css` 和 `app.js` 增加静态版本参数 | 新 URL、三视口网格和视频状态 | pass |
| R5 / 3 | 场景章节只有四张静态卡，无法观察输入、输出和产品边界 | 场景只被当作研究结论，没有交互状态或示例 | 新增六场景 tab、动态输入/输出/仍需字段、网页预览和总结卡 | 六场景状态、截图、README | pass |
| R5 / 5 | 完整效果片无法可靠跳到指定时间点 | Python 静态服务器不提供可用的媒体 Range seek | 从原片裁出五条独立 540×960 H.264＋AAC 场景短片，在面板内切换播放 | ffprobe、五短片播放、错误回退 | pass |
| R5 / 5 | 初次裁出的字族短片结束后停在下一章跟写画面 | 裁切终点与下一章首帧重合 | 将五段终点分别提前约 0.3 秒并保留章节末帧 | 五段首尾抽帧、浏览器字族播放 | pass |
| R5 / 7 | 手机锚点跳转后演示台标题被固定页头遮住 | `#scenario-lab` 没有滚动边距 | 增加 `scroll-margin-top: 96px` | 390px 锚点、相邻章节 | pass |
| R6 / 3 | “先做内容工作台”仍只是路线建议，无法验证生产路径 | 内容参数仍散落在组件代码中 | 新增五字配置目录、原生表单、即时预览、JSON 下载和配置样片 | 目录、Composition、视频、页面 | pass |
| R6 / 5 | 静态页面读取目录或样片失败时可能让用户误以为整个工具不可用 | 网络资源与编辑状态没有解耦 | 目录失败启用内置一字回退；样片失败保留表单与 JSON 导出 | 缺失地址、重试恢复、表单可用性 | pass |
| R6 / 7 | 工作台三栏在平板和手机无法保持合理输入宽度 | 桌面列数直接压缩会使表单和竖屏视频过窄 | 820px 以下整体单列，420px 以下表单行、按钮和结果区继续单列 | 768/390 尺寸、无横向溢出 | pass |
| R7 / 3 | “姓名祝福”只有方向描述，用户无法理解怎样完成一份礼物 | 没有面向普通用户的输入—预览—成片闭环 | 新增姓名、场景、风格、祝福、署名、日期、颜色表单和绚丽预览 | 主路径、真实样片、产品边界 | pass |
| R7 / 5 | 任意姓名网页预览容易被误解为都能真实逐笔渲染 | 字体预览与已接入路径没有显式区分 | 在预览旁持续说明字体边界；真实视频固定显示“沐、阳”13 笔证据 | 默认、换名、错误媒体 | pass |
| R7 / 7 | 星光舞台和三栏产品区在窄屏有拥挤与持续动画风险 | 桌面构图直接压缩且粒子默认无限循环 | 820px 以下单列、420px 以下控件和结果卡单列；reduced-motion 关闭星光与字形入场 | 768/390、reduced-motion | pass |
| R8 / 3 | 朋友婚礼信息仍只是对话中的创意建议 | 通用姓名 MVP 没有真实新人、地点和仪式叙事 | 新增张帅、董小姐、2026.09.12、延安共享配置、五幕舞台与三端交付包 | 配置、页面、真实成片 | pass |
| R8 / 5 | 完整展示名容易被误解为每个字都已接入真实路径 | 当前只导入“张／董”笔画，董小姐完整姓名未知 | 在页面、JSON、视频和 README 明确“张／董”真实路径、“帅／小姐”称谓排版与隐私假设 | 关键帧、下载配置、边界文本 | pass |
| R8 / 7 | 初版 16:9 直接压缩竖版层级，称谓、祝福和日期拥挤 | 横版仍沿用竖版的纵向定位 | 横版改为左侧称谓祝福、右侧两姓笔画、底部日期地点的独立构图 | 1440 横版定格、相邻竖版 | pass |
| R8 / 7 | 390px 内嵌横版的誓言与底部日期重叠约 13px | 极窄横版仍使用桌面横版字号和间距 | 手机横版单独缩小称谓与誓言并上移，保留全部信息 | 元素边界、无横向溢出 | pass |
| R9 / 3 | 婚礼完整信息与真实事实混在同一阅读流时可能被误认 | 用户只提供了称谓、日期和地点，其余资料需要模拟 | 新增独立 `DEMO_ONLY` 数据层，并在请帖、视频、海报、RSVP 持续显示模拟／替换说明 | JSON、DOM、视频第 18 帧、海报第 19 帧 | pass |
| R9 / 5 | 大屏视频故障注入后仍显示“视频已就绪” | Chromium 的 `<source>` 错误不向 `<video>` 冒泡 | 同时监听每个 `<source>` 的 `error` 并复用媒体回退状态 | 缺失 MP4、请帖／海报／RSVP 保持可用、恢复加载 | pass |
| R9 / 7 | 动态请帖的三张横排照片在手机上会过窄 | 桌面三列叙事直接压缩 | 420px 下改为三张纵向照片卡，事实、RSVP 操作和替换清单同步单列 | 390×844 截图、无横向溢出 | pass |
| R10 / 3 | 抽象照片位无法验证视觉连续性和多端复用 | 只有结构占位，没有可消费的故事素材 | 生成同一对完全虚构成年人的“初见／同行／约定”三张图片，接入请帖、大屏和迎宾海报 | 源图、WebP、配置、视频第 22 帧、网页第 23／24 帧 | pass |
| R10 / 5 | AI 图片加载失败时请帖可能出现空洞 | 新图片成为叙事主视觉，网络错误必须可恢复 | 图片 `error` 后为卡片添加 `is-image-error`，隐藏坏图并显示原抽象占位 | 故障注入、其余图片／视频／RSVP、刷新恢复 | pass |
| R10 / 7 | 三张大图和四张结论卡在手机上可能过窄 | 新素材探索区沿用桌面三／四列网格 | 420px 下素材、结论和下载入口全部单列 | 390×844、加载尺寸、无横向溢出 | pass |

## 视口与布局证据

| 视口 | 首屏观察 | DOM / 尺寸证据 | 结果 |
| --- | --- | --- | --- |
| 1440×900 | 工作台为表单、即时预览、真实样片三栏；其余五维总览和六场景结构保持 | 目录为 5 字／4 场景／3 模板；配置样片 `readyState=4`、`error=null`、8.55 秒；无横向溢出 | pass |
| 768×1024 | 工作台标题、表单、预览和真实样片单列，保持完整输入宽度 | 工作台各主卡宽约 721px；文档无横向溢出 | pass |
| 390×844 | 工作台表单行、操作按钮、预览和真实样片全部单列 | 主卡宽约 343px、输入区宽约 305px、视频宽约 238px；无横向溢出 | pass |
| 1440×900 / 姓名产品 | 表单、绚丽预览、真实“沐阳”样片三栏；主标题与产品动作在首次扫描中明确 | 配置为 4 场景／3 风格；姓名视频 `readyState=4`、12.544 秒；无横向溢出 | pass |
| 768×1024 / 姓名产品 | 标题、表单、预览和视频卡完整单列 | 表单、预览、视频卡均约 721px；无横向溢出 | pass |
| 390×844 / 姓名产品 | 两列表单行、操作按钮、预览底栏、视频和四场景卡均转为单列 | 主卡约 343px、输入区约 301px、视频约 238px；无横向溢出 | pass |
| 1440×900 / 婚礼案例 | 左侧事实、画幅、五幕与操作；右侧持续舞台；下方真实成片和三端交付 | 案例为 5 幕／3 种交付；横版定格无重叠；婚礼视频 `readyState=4`、20 秒；无横向溢出 | pass |
| 768×900 / 婚礼案例 | 舞台先出现，控制区随后，真实成片保持完整阅读宽度 | 案例约 721px；舞台 `grid-row=1`、控制 `grid-row=2`；无横向溢出 | pass |
| 390×844 / 婚礼案例 | 标题、竖版舞台、事实、画幅、五幕、操作和三端交付全部单列 | 案例约 351px、舞台与控制约 317px；横版内嵌修复后也无重叠和横向溢出 | pass |
| 1440×900 / 完整婚礼交付 | 左侧模拟照片版请帖，右侧 16:9 真实播放器；模拟声明在首屏持续可见 | 8 个 DOM 视频；大屏 `1920×1080`、25 秒；迎宾图 `1080×1350`；无横向溢出 | pass |
| 768×900 / 完整婚礼交付 | 请帖、大屏、海报和 RSVP 改为完整单列，大屏卡取消 sticky | 交付区约 658px；`position=relative`；无横向溢出 | pass |
| 390×844 / 完整婚礼交付 | 标题、模拟声明、请帖姓名和第一张照片在首屏顺序清晰，后续全部单列 | 交付区约 317px；照片与 RSVP 操作单列；媒体 `readyState=4`；无横向溢出 | pass |
| 1440×1100 / R10 素材探索 | 三张 AI 故事图保持三列，四项结论和三个下载入口完整可见 | 三张 WebP 均 `720×900`；大屏 `1920×1080`、25 秒；迎宾图 `1080×1350`；无横向溢出 | pass |
| 390×844 / R10 素材探索 | 三张故事图、四项结论和下载入口顺序单列 | 素材与结论列宽均约 317px；三图加载完成；无横向溢出 | pass |

## 交互、状态与可访问性

| 检查 | 证据 | 结果 |
| --- | --- | --- |
| 页面非空和无错误覆盖层 | 页面包含 `#overview`、`#our-demo`、`#scenario-lab`、`#workbench`、`#name-blessing`、`#wedding-case`、`#wedding-complete-pack`、`#effects-lab`、`#decision`、七条主视频和场景证据播放器；未发现错误覆盖层 | pass |
| 控制台和页面错误 | `agent-browser console` 与 `agent-browser errors` 无输出 | pass |
| 七视频正常状态 | “懒”“忙”、效果实验室、配置样片、姓名祝福、婚礼故事和婚礼大屏均可加载；大屏片为 1920×1080、`readyState=4`、`error=null`、25.000 秒 | pass |
| 六场景切换 | 文化短视频、识字课件、祝福/品牌、文化展陈、字族内容、互动跟写均同步更新标题、输入、输出、仍需、预览和边界 | pass |
| 场景键盘路径 | tablist 中 ArrowRight 从文化短视频移动到识字课件；选中、焦点和 `aria-labelledby` 同步，Home/End 与反向键已实现 | pass |
| 网页示例播放 | 点击后 `scenarioChar`、部件、字幕和进度动画进入播放态；完成后状态文本更新；reduced-motion 下动画为 `0.01ms` 且信息保持可见 | pass |
| 场景证据短片 | 五条短片均可在面板内切换；字族场景显示清、情、晴、请画面；场景短片与七条主视频保持互斥播放 | pass |
| 场景短片错误与恢复 | 缺失短片时播放器隐藏并显示“网页示例和能力边界仍可使用”；切回识字场景后短片恢复 `readyState=4` | pass |
| 工作台目录主路径 | 初始为“明／展陈／深蓝”；选“安”自动带出祝福场景和目录文案；切黑板模板后强调色为 `#f1c84b`，预览 token 同步更新 | pass |
| 工作台编辑与校验 | 标题、文案即时映射到预览；空标题设置 `aria-invalid=true` 并禁用下载；恢复后重新启用 | pass |
| 工作台重置与下载 | 重置恢复当前目录字的默认场景和文案；下载文件名为 `hanzi-安-workbench.json`，字段与 Composition props 同构 | pass |
| 工作台目录错误与恢复 | 缺失目录时启用 1 字内置配置，表单仍可编辑且显示重试；恢复真实地址后回到 5 字／4 场景／3 模板 | pass |
| 工作台视频错误与恢复 | 缺失样片触发错误码 4，并提示“表单与 JSON 导出仍可使用”；恢复地址后回到 `readyState=4` | pass |
| 姓名产品主路径 | 将“沐阳”改为“林悦”，选择婚礼与烟火绮梦后，祝福自动变为“愿并肩看尽山河，共赴长久。”，预览字、署名、日期和 token 同步 | pass |
| 姓名输入校验 | `A悦` 设置 `aria-invalid=true` 并禁用下载；恢复 2–4 个汉字后重新可用 | pass |
| 姓名重播、重置与导出 | Enter 聚焦重播按钮并保留焦点；重置恢复完整“沐阳”示例；导出反馈显示 `name-blessing-沐阳.json` 及渲染服务边界 | pass |
| 姓名配置错误与恢复 | 缺失目录时启用 1 场景／1 风格内置示例且表单可编辑；恢复真实目录后回到 4 场景／3 风格 | pass |
| 姓名视频错误与恢复 | 缺失样片触发错误码 4，并提示“配置与即时预览仍可使用”；恢复后回到 `readyState=4` | pass |
| 婚礼五幕主路径 | 自动播放从序章推进到祝福定格并停留；暂停恢复按钮状态；点击任意一幕立即更新标题、描述、视觉状态和进度 | pass |
| 婚礼画幅与键盘 | 9:16／16:9 切换保持新人、日期、地点和誓言；ArrowRight 从延安序章移动到两姓入场，选中、焦点和舞台同步，`:focus-visible=true` | pass |
| 婚礼导出与配置回退 | 导出反馈为 `wedding-张帅-董小姐-20260912.json`；缺失配置启用内置 5 幕案例且下载仍可用，真实地址恢复后回到 5 幕／3 交付 | pass |
| 婚礼视频错误与恢复 | 直接切换缺失地址触发错误码 4，提示“互动分镜和项目配置仍可使用”；恢复后回到 `readyState=4` 和 20 秒 | pass |
| 模拟请帖内容 | 新娘模拟全名、倒计时、11:58、演示场地／地址／着装／电话、三张 `SIMULATED PHOTO` 和三段演示时间线均由共享 JSON 映射 | pass |
| RSVP 空状态校验 | 原生提交空表单后同时设置姓名与出席 `aria-invalid=true`，状态列出两个错误，焦点回到 `#rsvp-name` | pass |
| RSVP 成功、键盘与重置 | “王明／欣然赴约／2 位／祝福”生成本地回执；“键盘宾客／遥寄祝福”可由输入框 Enter 提交并把焦点移到结果；均明确未提交／发送／保存；reset 清空状态 | pass |
| 婚礼完整配置回退 | 请求缺失 JSON 后启用内置案例；董雅宁、演示场地、3 张照片与 3 段时间线仍存在，重试按钮可见 | pass |
| 大屏视频错误与恢复 | 缺失 `<source>` 后显示“大屏成片未加载，模拟请帖、迎宾海报和 RSVP 仍可使用”；重新加载正常地址后恢复 25 秒和 1920×1080 | pass |
| 播放互斥 | 姓名样片播放后启动婚礼样片，前者由 `paused=false` 变为 `true`，婚礼播放且其余播放器均暂停 | pass |
| 原生媒体控件 | 七条主视频和场景证据播放器均保留浏览器原生播放、进度、音量、静音和全屏控件 | pass |
| 产品方向折叠项 | `<summary>` 可聚焦；Enter 将首项从 `open=true` 切换为 `false`，焦点保留 | pass |
| 焦点可见 | 全站 `:focus-visible` 使用 3px 黄色轮廓和 4px 偏移 | pass |
| reduced-motion | 浏览器模拟后 media query 为 true；婚礼重播直接进入祝福定格，状态明确说明降级，舞台 `transition-duration=0.01ms`，姓名、日期、地点与誓言保持可见 | pass |
| R9 reduced-motion | 模拟照片卡 `animation-duration=0.01ms`、婚礼舞台 `transition-duration=0.01ms`；完整请帖标题和全部资料仍在 DOM 可读 | pass |
| R10 图片加载与边界 | 请帖和素材探索的六个图片节点均加载为 720×900；三项 `AI SIMULATED` 标签可见；样例清单下载入口存在 | pass |
| R10 图片错误回退 | 将第一张请帖图片切为缺失资源后，父卡变为 `is-image-error`、图片 `display:none`、抽象占位 `display:block`；刷新后恢复 | pass |
| R10 reduced-motion | 浏览器 media query 为 true；素材图 `transition-duration=0.01ms`、`animation-duration=0.01ms`，探索区无隐藏内容 | pass |
| 视频错误回退 | 将效果实验室切换到缺失地址后触发错误码 4；状态显示“效果视频未加载，请使用右侧下载链接”，后续产品章节仍存在 | pass |

## 性能观察

agent-browser 本地 Page Load Profile：

- TTFB：0.7ms
- FCP：136ms
- LCP：136ms，元素为上游视频海报 `poster.webp`
- CLS：0
- 七条主 MP4 合计约 15.59MB，五条场景短片合计约 0.59MB；主视频和当前场景播放器使用 `preload="metadata"`，其余短片仅在选择时请求，不自动播放

这些数据用于发现明显本地性能问题，不代表公网 GitHub Pages 的真实网络表现。

## 不适用边界

- Theme：设计契约明确只支持固定编辑主题，没有 light/dark 切换状态；浅蓝章节是内容分区而非主题状态。
- Locale：本页是固定中文研究报告，不提供语言切换或可编辑本地化控件。
- Dialog：静态单页没有前景弹层。工作台已有目录 loading／ready／fallback、表单 invalid／valid、下载和媒体错误状态，均已验证。

## 终端审计

- 上游源码、七条主 MP4、五条场景证据短片、三张 AI 源图与优化图、24 份视觉证据、六场景演示、五字工作台、姓名祝福 MVP、婚礼三端交付样板、产品路线和复现文档均存在。
- 1440/768/390 三个目标视口、R10 的 1440×1100／390×844 素材探索视口、姓名与工作台表单、婚礼五幕、模拟请帖、照片版横屏、迎宾海报与本地 RSVP 主路径、图片／配置／视频错误恢复、键盘、互斥播放及 reduced-motion 均有浏览器证据。
- 没有剩余 `continue`、`defer` 或 `blocked` 项。
